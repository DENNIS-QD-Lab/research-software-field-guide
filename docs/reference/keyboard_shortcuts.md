# Keyboard Shortcuts

VS Code shortcuts that make daily work faster. On Mac use `Cmd`; on Windows use `Ctrl` unless noted otherwise. The two are interchangeable in nearly every shortcut below.

You don't need to memorize these. Pick three or four that solve a friction you currently feel, learn those, then come back for more when you notice yourself doing something repeatedly with the mouse.

(Hot tip-- if you're looking at this in VS code, use Cmd+Shift+V to make the markdown render so that it's readable! (and that's your first hotkey win! smile))

## File and project navigation

| Action | Mac | Windows |
|---|---|---|
| Go to File (fuzzy search by filename) | `Cmd+P` | `Ctrl+P` |
| Find in Files (search file contents) | `Cmd+Shift+F` | `Ctrl+Shift+F` |
| Replace in Files | `Cmd+Shift+H` | `Ctrl+Shift+H` |
| Go to Symbol in File (functions, classes) | `Cmd+Shift+O` | `Ctrl+Shift+O` |
| Go to Symbol in Workspace | `Cmd+T` | `Ctrl+T` |
| Switch between open editors | `Cmd+1`, `Cmd+2`, etc. | `Ctrl+1`, `Ctrl+2`, etc. |
| Close current editor | `Cmd+W` | `Ctrl+W` |
| Reopen closed editor | `Cmd+Shift+T` | `Ctrl+Shift+T` |

## Within a file

| Action | Mac | Windows |
|---|---|---|
| Find in current file | `Cmd+F` | `Ctrl+F` |
| Replace in current file | `Cmd+Option+F` | `Ctrl+H` |
| Go to line number | `Cmd+G` | `Ctrl+G` |
| Comment / uncomment line | `Cmd+/` | `Ctrl+/` |
| Comment / uncomment block | `Cmd+Option+/` | `Ctrl+Shift+/` |
| Duplicate line down | `Shift+Option+Down` | `Shift+Alt+Down` |
| Move line up / down | `Option+Up` / `Option+Down` | `Alt+Up` / `Alt+Down` |
| Delete line | `Cmd+Shift+K` | `Ctrl+Shift+K` |
| Select all occurrences of selection | `Cmd+Shift+L` | `Ctrl+Shift+L` |
| Add cursor to next occurrence | `Cmd+D` | `Ctrl+D` |
| Indent / outdent line | `Tab` / `Shift+Tab` | `Tab` / `Shift+Tab` |

## Panels and UI

| Action | Mac | Windows |
|---|---|---|
| Toggle sidebar | `Cmd+B` | `Ctrl+B` |
| Toggle terminal | ``Ctrl+` `` | ``Ctrl+` `` |
| Toggle problems panel | `Cmd+Shift+M` | `Ctrl+Shift+M` |
| Open Command Palette (run any command by name) | `Cmd+Shift+P` | `Ctrl+Shift+P` |
| Zen mode (distraction-free) | `Cmd+K Z` | `Ctrl+K Z` |
| Split editor right | `Cmd+\` | `Ctrl+\` |

## Markdown and notebooks

| Action | Mac | Windows |
|---|---|---|
| Markdown preview (side-by-side) | `Cmd+Shift+V` | `Ctrl+Shift+V` |
| Markdown preview (replace editor) | `Cmd+K V` | `Ctrl+K V` |
| Run notebook cell | `Shift+Enter` | `Shift+Enter` |
| Run cell and insert below | `Option+Enter` | `Alt+Enter` |
| Add cell above / below | `A` / `B` (when cell not in edit mode) | same |
| Convert cell to markdown | `M` (when cell not in edit mode) | same |
| Convert cell to code | `Y` (when cell not in edit mode) | same |

## Git

| Action | Mac | Windows |
|---|---|---|
| Open Source Control panel | `Ctrl+Shift+G` | `Ctrl+Shift+G` |
| Stage selected lines (in diff view) | right-click → "Stage Selected Ranges" | same |

Most Git work happens through the Source Control panel UI rather than keyboard shortcuts. The shortcut to remember is the panel itself.

## Discovering shortcuts you don't know yet

Two tricks worth knowing:

1. **The Command Palette is searchable.** Press `Cmd+Shift+P` and start typing what you want to do ("format document", "open settings", "rename symbol"). The palette shows the matching commands *and* their keyboard shortcuts on the right. Use this when you know what you want to do but not how to trigger it.

2. **The Keyboard Shortcuts editor lets you search by action.** `Cmd+K Cmd+S` (Mac) or `Ctrl+K Ctrl+S` (Windows) opens a searchable list of every action and its shortcut. You can also reassign shortcuts here if a default conflicts with another tool you use.